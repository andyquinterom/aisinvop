
problema <- function(){
  modelo <- structure(list(variables = list(),
                           variables_tipo = list(),
                 indices_variables = list(),
                 funcion_objetivo = NULL,
                 restricciones = list()))
return(modelo)
}